from flask import Flask, jsonify
from flask_cors import CORS

# --------------------------
# 🔹 बेसिक Flask सेटअप
# --------------------------
# यहाँ हम Flask का एक ऐप बना रहे हैं और CORS (Cross-Origin Resource Sharing) को चालू कर रहे हैं
# ताकि यह API किसी भी जगह से एक्सेस की जा सके।
app = Flask(__name__)
CORS(app)

# --------------------------
# 📌 होम रूट (टेस्ट करने के लिए)
# --------------------------
# जब कोई ब्राउज़र में "/" (होम पेज) खोलेगा, तो यह फंक्शन चलेगा
# और एक साधारण JSON मैसेज दिखाएगा।
@app.route("/")
def home():
    return jsonify({"message": "🚀 Flask API चल रही है भाई!"})

# --------------------------
# 📌 उदाहरण रूट
# --------------------------
# यह रूट URL में नाम लेता है (जैसे /hello/Shawez)
# और एक मैसेज में उस नाम को दिखाता है।
@app.route("/hello/<name>")
def hello(name):
    return jsonify({"message": f"Hello {name}, Flask API बिल्कुल सही काम कर रही है!"})

# --------------------------
# 🚀 सर्वर चलाने का कमांड
# --------------------------
# यह हिस्सा बताता है कि ऐप को 0.0.0.0 होस्ट और 10000 पोर्ट पर चलना है।
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=10000)
