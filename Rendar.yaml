services:
  - type: web
    name: my-flask-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    branch: main
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
